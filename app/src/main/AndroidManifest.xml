<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.smarttersstudio.crimemanagementsystem">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MenuActivity"
            android:theme="@style/NoBar" />
        <activity android:name=".CrimeActivity"
            android:label="Crime List"/>
        <activity android:name=".MissingActivity"
            android:label="Missing List" />
        <activity android:name=".LoginActivity"
            android:theme="@style/NoBar" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".SignupActivity"
            android:theme="@style/NoBar" />
        <activity android:name=".ForgotPasswordActivity"
            android:label="Reset Password"/>
        <activity android:name=".MyMissingActivity"
            android:label="My Missing List"/>
        <activity android:name=".MyCrimeActivity"
            android:label="My Crime List" />
        <activity android:name=".MyComplainActivity"
            android:label="My Complain List" />
        <activity android:name=".AddMyCrimeActivity"
            android:label="Add Crime"/>
        <activity android:name=".AddMyComplainActivity"
            android:label="Add Complain" />
        <activity android:name=".AddMyMissingActivity"
            android:label="Add Missing Person"/>
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Base.Theme.AppCompat" />
    </application>

</manifest>